// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCxXrwfVt8dMEnk21ARyQrGhEQAB5uQtv0",
    authDomain: "livelyforlivestock.firebaseapp.com",
    databaseURL: "https://livelyforlivestock.firebaseio.com",
    projectId: "livelyforlivestock",
    storageBucket: "livelyforlivestock.appspot.com",
    messagingSenderId: "693408095979",
    appId: "1:693408095979:web:4a4a22c6fd6d7cc4c728ae"
  },

  dialogflow: {
    Idkman: "100431275612501262594"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
